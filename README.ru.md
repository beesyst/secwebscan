# 🛡️ BeeScan

**BeeScan** — модульная платформа для комплексного аудита безопасности IT-инфраструктуры. Поддерживает подключение внешних инструментов в виде плагинов, автоматический сбор и структурирование результатов, генерацию отчетов в различных форматах и гибкую централизованную настройку. Архитектура BeeScan позволяет анализировать сеть, веб-приложения, DNS и API, а также масштабировать систему под любые задачи DevSecOps, пентеста и мониторинга.

## ⚙️ Основные возможности

- **Поддержка плагинов** — 5 инструментов.  
- **Plug-and-Play архитектура** — каждый инструмент вынесен в отдельный модуль-парсер.  
- **Вывод в PostgreSQL** и рендер отчетов из БД.  
- **Тул-профили** — выбирай уровень обработки сканера в каждом инструменте.  
- **Генерация отчетов**: TERMINAL, HTML, PDF.  
- **Изоляция через Docker** — отдельные контейнеры для ядра и БД.  
- **Логирование** — отдельные логи для хоста и контейнера.
- **Мультиязычность** - бот поддерживает **переключение языка** через `config.json` и добавление новых языков в `lang.json`.

## 🌍 Где можно использовать

- **Пентесты и тесты на проникновение**  
- **Аудит инфраструктуры и веб-сервисов**  
- **Безопасность государственных и корпоративных сетей**  
- **DevSecOps и CI/CD**

## 🛠️ Технологический стек

- **Python** — основной язык разработки  
- **PostgreSQL** — база данных  
- **Docker** — контейнеризация среды  
- **Jinja2** — шаблоны отчетов  
- **Rich** — таблицы в терминале  
- **WeasyPrint** — генерация PDF

### 🔌 Подключенные инструменты

| Инструмент | Описание | Версия |
|------------|----------|--------|
| `nmap`     | Сканирование сети, обнаружение портов, сервисов и версий. | ![v](https://img.shields.io/badge/nmap-stable-blue) |
| `nikto`    | Анализ веб-серверов, поиск конфигурационных ошибок и уязвимостей. | ![v](https://img.shields.io/badge/nikto-2.5.0-blue) |
| `dig`      | Получение и анализ DNS-записей, определение зоны ответственности. | ![v](https://img.shields.io/badge/dig-bind9-blue) |
| `nuclei`   | Проверка на наличие шаблонных уязвимостей по YAML-шаблонам. | ![v](https://img.shields.io/badge/nuclei-v3.4.3-blue) |

## 🖧 Архитектура

### Компоненты системы

1. **Плагины (`plugins/*.py`)** — модули-обертки над CLI-инструментами (например, `nmap`, `nikto`). Каждый плагин реализует функции:
   - `scan()` — запуск сканера и сохранение пути к файлу результата (`.xml`, `.json`);
   - `parse()` — парсинг результатов;
   - `merge_entries()` — объединение данных по IP и Domain в `source: "Both"`;
   - `get_column_order()` и `get_wide_fields()` — настройка визуального отображения.
2. **Runner (`plugin_runner.py`)** — запускает плагины, сохраняет пути к их результатам во временный JSON (`/tmp/temp_files_*.json`), не сохраняет содержимое файлов.
3. **Коллектор (`collector.py`)** — загружает пути файлов, вызывает `parse()` / `merge_entries()`, фильтрует неинформативные строки и сохраняет результат в таблицу `results`.
4. **База данных (PostgreSQL)** — централизованное хранилище для всех результатов, все плагины пишут в одну таблицу `results` с полем `plugin` для идентификации.
5. **Генератор отчетов (`report_generator.py`)** — извлекает данные из БД, группирует по категориям, визуализирует:
   - терминальный отчет (rich),
   - HTML (Jinja2 + CSS),
   - PDF (через WeasyPrint).
6. **Конфигурационный модуль (`config/config.json`)** — определяет цели сканирования (`target_ip`, `target_domain`), активные плагины, уровни сложности, формат отчетов, тему (`light` / `dark`) и поведение (`open_report`, `clear_db` и др.).
7. **Оболочка запуска (`start.py`)** — единая точка входа, управляет запуском Docker-среды, БД, сканеров, сбором и генерацией отчетов с прогресс-индикаторами.
8. **Docker-среда** — изолированное и полностью самодостаточное окружение:
   - `beescan_base` — контейнер со всеми сканерами и логикой,
   - `postgres` — отдельный контейнер с базой данных,
   - `beescan_network` — мостовая сеть между компонентами.

### Структура проекта

``` 
beescan/
├── config/                  # Конфигурация
│   ├── config.json          # Основной конфигурационный файл
│   └── start.py             # Главный скрипт запуска проекта (на Python)
├── core/                    # Ядро системы
│   ├── collector.py         # Парсит результаты и сохраняет в БД
│   ├── logger_container.py  # Логгер для записи из контейнера
│   ├── logger_host.py       # Логгер для записи на хосте
│   ├── plugin_runner.py     # Запускает активные плагины
│   └── report_generator.py  # Генерирует отчеты из БД
├── db/                      # База данных (PostgreSQL)
│   ├── compose.yaml         # Docker Compose конфигурация
│   ├── Dockerfile           # Dockerfile для создания образа PostgreSQL (опционально)
│   ├── init.sql             # Скрипт инициализации таблиц
│   └── populate_db.py       # Скрипт ручной вставки тестовых данных
├── docker/                  # Docker-инфраструктура
│   ├── Dockerfile.base      # Dockerfile базового контейнера beescan-base
│   └── install_plugins.py   # Скрипт установки CLI-инструментов в контейнер
├── logs/                    # Логи запуска и работы
│   ├── container.log        # Лог из контейнера
│   └── host.log             # Лог из хоста
├── plugins/                 # Плагины-сканеры
│   ├── nmap.py              # Парсер Nmap
│   └── nikto.py             # Парсер Nikto
├── reports/                 # Сгенерированные HTML и PDF отчеты
├── templates/               # Шаблоны Jinja2 для отчетов
│   ├── css/                 # Папка css-файлами
│   └── report.html.j2       # HTML-шаблон для генерации отчета
├── README.md                # Документация проекта
├── requirements.txt         # Зависимости Python
└── start.sh                 # Bash-скрипт для авто-запуска всех компонентов
```

## ⚙️ Pipeplan: Как это работает?

### Запуск системы

1. Система запускается через `start.sh`.
2. Проверяется наличие Docker и сети `beescan_network`.
3. Запускается контейнер PostgreSQL (если не активен).
4. Собирается образ `beescan-base` (если не создан).
5. Поднимается контейнер `beescan_base` с монтированными папками.
6. Запускается `plugin_runner.py` для сканирования целей.
7. Сохраняются пути к результатам в временный JSON-файл.
8. Запускается `collector.py` для обработки результатов и записи в БД.
9. Генерируются отчеты: `terminal`, `html`, `pdf`.

### Работа плагинов

1. `plugin_runner.py` читает `config.json` и определяет активные плагины.
2. Запускает сканирование через функцию `scan()` в каждом плагине (`plugins/*.py`).
3. Сохраняет пути к результатам (`XML`/`JSON`) во временный файл.
4. Для каждого плагина вызывается функция `parse()` для разбора данных.

### Сбор данных (`collector.py`)

1. Коллектор подключается к БД.
2. Очищает таблицу `results`, если включено `"clear_db": true`.
3. Загружает плагин-парсер из `plugins/*.py`.
4. Обрабатывает все временные файлы (`temp_files_*.json`).
5. Вызывает `parse()` и `merge_entries()`.
6. Фильтрует пустые или неинформативные записи.
5. Структурированные данные записываются в одну таблицу `results`.

### Генерация отчетов (`report_generator.py`)

1. Извлекает данные из таблицы `results`.
2. Автоматически определяет категории (`Network Security`, `Application Security` и т.д.).
3. Для каждого плагина подгружает порядок колонок (`get_column_order()`) и широкие поля (`get_wide_fields()`).
4. Генерирует:
   - Терминальный отчет (`rich`-таблицы),
   - HTML отчет (через `Jinja2` шаблон `report.html.j2`),
   - PDF отчет (на базе HTML через `WeasyPrint`).
5. HTML-отчет автоматически открывается в браузере, если `"open_report": true` в `config.json`.
6. Поддерживается выбор темы: `"light"` или `"dark"`.

### Пример работы Nmap

1. В `config.json` выбран модуль `nmap`, установлен уровень сложности (например, `middle`).
2. Запускается Nmap с аргументами уровня (`-T4 -sS -sV -Pn --open` и т.д.).
3. Сканы по IP и Domain сохраняются отдельно.
4. Плагин `nmap.py`:
   - Парсит XML,
   - Объединяет результаты IP/Domain через `merge_entries()`,
   - Передает данные в коллектор.
5. Все записи добавляются в таблицу `results`.
6. `report_generator.py` выводит результаты в терминал, HTML и PDF с правильным порядком колонок.

## ▶️ Установка и запуск

### Запуск проекта

```bash
git clone https://github.com/beesyst/beescan.git
cd beescan
bash start.sh
```

В процессе установки нужно будет выбрать язык установки.

## 🔧 Настройка конфигурации

Все параметры задаются в файле `config.json`:

| Параметр         | Значение по умолчанию | Что делает `true`                                                                 | Что делает `false`                                               |
|------------------|------------------------|-----------------------------------------------------------------------------------|------------------------------------------------------------------|
| `target`         | `"1.1.1.1"`            | Сканирует указанный IP или домен                                                 | —                                                                |
| `report_formats` | `["terminal", "html"]`             | Генерирует указанные форматы: `terminal`, `html`, `pdf`                          | —                                                                |
| `open_report`    | `true`                 | Авто-открытие HTML и PDF-отчета в браузере                                       | Отчет не открывается автоматически                               |
| `clear_logs`     | `true`                 | Очищает `host.log` и `container.log` при запуске                            | Логи сохраняются и накапливаются                                |
| `clear_reports`  | `true`                 | Удаляет старые отчеты из папки `reports/`                                        | Старые отчеты сохраняются                                       |
| `clear_db` | `true`                 | Полностью очищает БД перед сканированием                     | Старые результаты остаются в БД                                |
| `report_theme`   | `"dark"`               | Используется темная тема в HTML- и PDF-отчете                                           | `"light"` — светлая тема отчета                                  |

## 💰 Донат

- **USDT (TRC20)**/**USDC (TRC20)**: `TUQj3sguQjmKFJEMotyb3kERVgnfvhzG7o`
- **SOL (Solana)**: `6VA9oJbkszteTZJbH6mmLioKTSq4r4E3N1bsoPaxQgr4`
- **XRP (XRP)**: `rDkEZehHFqSjiGdBHsseR64fCcRXuJbgfr`

---

**🛡 Licensed for non-commercial use only. See [LICENSE](LICENSE) for details.**